New prompt in testing ascending CVaR and Quantum Tilted Loss.

Write a Python file that can generate a Jupyter Notebook with '.ipynb' that can do the following: 1. I want to reproduce the problem and solutions in https://github.com/ioankolot/Ascending_CVaR. 2. Change the loss functon to the quantum tilted loss function defined by $\mathcal{L}_\gamma(H,\rho(\theta)) := - \frac{1}{\gamma} \log \text{tr} \left( e^{- \gamma H} \rho(\theta) \right)$, where $\gamma$ is a real parameter. As $\gamma \to 0$, it recovers the standard expectation value. For $\gamma > 0$, it penalizes high-energy (sub-optimal) states more heavily, potentially accelerating convergence to the ground state. 3. Compare Standard Loss function, CVaR loss function, and the defined Tilted Loss for the performance of problem solving. 4. Compare the algorithm performance with different fixed values of $\gamma$ (0.1, 5, 10, 20, 100). 5. Investigate the effect of tuning $\gamma$ using different schedules (Linear, Sigmoid, Exponential, Logarithmic), in a way similar to the paper: https://arxiv.org/pdf/2105.11766, and compare the performance with assending CVaR. Be mindful that the tuning $\gamma$ is done in the same way as the tuning $\alpha$ in the paper. 


Directly transform 'generate_tilted_cvar_comparison.py' file into a Jupyter Notebook 'tilted_cvar_comparison.ipynb'.


Write a Python file that can do the following: 1. Use QAOA algorithm to solve a MaxCut problem. 2. Change the loss functon to the quantum tilted loss function defined by $\mathcal{L}_\gamma(H,\rho(\theta)) := - \frac{1}{\gamma} \log \text{tr} \left( e^{- \gamma H} \rho(\theta) \right)$, where $\gamma$ is a real parameter. As $\gamma \to 0$, it recovers the standard expectation value. For $\gamma > 0$, it penalizes high-energy (sub-optimal) states more heavily, potentially accelerating convergence to the ground state. 3. Compare Standard Loss function, CVaR loss function, and the defined Tilted Loss for the performance of problem solving. 4. Compare the algorithm performance with different fixed values of $\gamma$ (0.1, 5, 10, 20, 100). 5. Investigate the effect of tuning $\gamma$ using different schedules (Linear, Sigmoid, Exponential, Logarithmic) and compare the performance with assending CVaR. Be mindful that the tuning $\gamma$ is done in every iteration steps.. 
